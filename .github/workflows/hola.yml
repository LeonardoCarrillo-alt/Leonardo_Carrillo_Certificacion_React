name: descargando 
on:
   push:
   workflow_dispatch: 
jobs: 
  hola: 
    runs-on: ubuntu-latest
    steps: 
      - name: saludar
        run: echo "saludar"
      - name: Descargar código
        uses: actions/checkout@v4
      - name: ver carpetas
        run: pwd
      - name: ver archivos
        run: ls -la
